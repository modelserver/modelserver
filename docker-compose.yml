version: "3.8"
services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_DB: modelserver
      POSTGRES_USER: modelserver
      POSTGRES_PASSWORD: modelserver
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
